name: Validation Tests

on:
  push:
    branches: [development, main, "*-candidate"]
  pull_request:
    branches: [development, main, "*-candidate"]

permissions:
  contents: read

jobs:
  merge_requirements_tests:
    strategy:
      fail-fast: false
      matrix:
        poetry-version: ["1.8.2"]

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"

    - name: Poetry install
      uses: abatilo/actions-poetry@v2
      with:
        poetry-version: ${{ matrix.poetry-version }}

    - name: Install dependancies
      run: |-
        poetry install

    - name: Audit dependencies for known vulnerabilities
      # Non-blocking for now: a regression here should be visible without
      # failing the build until we've built confidence it isn't noisy.
      continue-on-error: true
      run: |-
        poetry run pip install pip-audit
        poetry run pip-audit

    - name: Test with pytest
      run: poetry run pytest --cov=instancespace --cov-report=term-missing

